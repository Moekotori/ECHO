# ECHO Next Library Core

This folder is reserved for the future SQLite-backed library core.

Phase 0 only defines the boundary. Scanning, metadata parsing, artwork lookup,
deduplication, and persistence must live here or in focused submodules under
this folder, not in `src/main/index.ts` or renderer app components.
