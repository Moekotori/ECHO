# Backend Lifecycle Matrix

Read-only snapshot of current backend lifecycle, freshness, recovery, fallback, and staleness behavior before backend abstraction. Preserve the outcomes below exactly unless a future task intentionally changes behavior with tests.

Reason-code categories used in `Condition`: `daemon_freshness`, `jsonrpc_transport`, `native_reuse`, `prepared_playback`, `asio_cooldown`, `operation_gate`, `run_token`, `watchdog`, `eq_sync`, `daemon_prefetch`, `shared_stability`, `dsp_replay`, `dsp_cache`, `startup_fallback`.

| Condition | Current Source (file:line) | Current Outcome | Preserved Outcome | Test Coverage Status |
| --- | --- | --- | --- | --- |
| `daemon_freshness.bridge_missing`: `ensureBackend()` is asked for daemon backend and `getActiveJsonRpcBridge()` returns null. | `AudioSession.ts:1548-1564` | Existing `DaemonAudioBackend` is disposed/nullified if present; no new backend is created; caller gets `null`. Callers use `recoverJsonDaemonBackend()`, which attempts `startAudioDaemon()` and either returns a recovered daemon backend or throws `json_daemon_unavailable:<action>` / `json_daemon_stale_backend:<action>`. | Preserve null backend return; callers use JSON daemon recovery or throw explicit `json_daemon_unavailable` / `json_daemon_stale_backend` errors. | Covered indirectly by AudioCore.test.ts integration suite (json daemon recovery/stale backend scenarios) |
| `daemon_freshness.bridge_closed`: active bridge exists but `jrpc.isClosed === true`, or existing daemon reports `isBridgeClosed`. | `AudioSession.ts:1544-1557`, `DaemonAudioBackend.ts:41-43` | Existing daemon wrapper is disposed and cleared; closed bridge is rejected for new daemon creation; caller gets `null`; playback proceeds through non-daemon path. | Preserve dispose-and-null behavior and closed-bridge rejection. | `JsonRpcBridge.test.ts:275-327` covers `isClosed` and post-close `rpc_bridge_not_open`; Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery scenarios) |
| `daemon_freshness.bridge_identity_changed`: `this.backend` is `DaemonAudioBackend` but not bound to the current active bridge. | `AudioSession.ts:1544-1548`, `DaemonAudioBackend.ts:37-39` | Old daemon wrapper is disposed and cleared; if current bridge is open and has `openFile`, a fresh `DaemonAudioBackend` is created and wired. | Preserve bridge identity as freshness boundary. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `daemon_freshness.bridge_matching_current`: current backend is `DaemonAudioBackend`, bridge is open, and `isBoundToBridge(jrpc)` is true. | `AudioSession.ts:1544-1550` | Existing daemon wrapper is returned unchanged; no EQ/DSP constructor replay occurs. | Preserve reuse of matching daemon backend. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `daemon_freshness.non_daemon_backend_present`: `this.backend` exists but is not a `DaemonAudioBackend`. | `AudioSession.ts:1551-1553` | Existing backend is returned without daemon bridge checks. | Preserve non-daemon backend passthrough. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `dsp_replay.fresh_daemon`: fresh daemon wrapper is constructed. | `AudioSession.ts:1538-1580` | DSP state cache is subscribed to the active bridge; persisted EQ state is loaded and sent with `jrpc.setState()` when truthy; channel balance is replayed only when `savedBalance.enabled`; room correction replays enable and trim only when `savedRoom.enabled`; all replay promises are fire-and-forget and errors are swallowed. | Preserve best-effort constructor replay with no playback-start failure on replay errors. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `dsp_cache.bridge_subscribed`: daemon backend is created after an active bridge appears. | `AudioSession.ts:1479-1496`, `AudioSession.ts:1553-1555` | Cache listeners for `eq.state`, `channelBalance.state`, and `roomCorrection.state` attach to the actual active bridge before daemon playback begins, even if no bridge existed when `AudioSession` was constructed. | Preserve bridge-aware DSP cache subscription for late-created daemon backends. | `AudioBackendContract.test.ts` covers late daemon bridge subscription. |
| `dsp_cache.bridge_changed_or_dispose`: cached daemon backend is disposed, active bridge changes, or session is disposed. | `AudioSession.ts:1488-1496`, `AudioSession.ts:1542-1545`, `AudioSession.ts:3112-3156` | Cache listeners are removed from the bridge they were actually attached to before subscribing to a new bridge or disposing the session. | Preserve listener ownership and avoid stale bridge cache updates/leaks. | `AudioBackendContract.test.ts` covers bridge swap listener removal. |
| `daemon_prefetch.low_load_enabled`: `getPlaybackLoadSettings().lowLoadPlaybackModeEnabled === true` during local prepare. | `AudioSession.ts:1617-1619`, `AudioSession.ts:1709-1723` | Prepare performs best-effort daemon prefetch through `ensureBackend()` when an active usable JSON-RPC bridge exists; failures are ignored/logged only in verbose mode. | Preserve low-load prepare prefetch as an optimization only, not as the playback routing gate. | Covered indirectly by AudioCore.test.ts prepare/prefetch tests. |
| `daemon_prefetch.low_load_disabled_or_settings_error`: low-load mode false, absent, or settings read throws during local prepare. | `AudioSession.ts:303-317`, `AudioSession.ts:1617-1619`, `AudioSession.ts:1709-1723` | Prepare skips best-effort daemon prefetch; local playback still tries an active usable JSON-RPC daemon bridge by default via `getBackend()`. | Preserve prefetch opt-in while keeping playback default JSON-RPC when the daemon bridge is active. | `AudioBackendContract.test.ts` covers default daemon playback with `lowLoadPlaybackModeEnabled=false`. |
| `jsonrpc_transport.play_resume`: `play()` sees `currentOutputBackend === 'jsonrpc'`, backend exists, and state is `paused`. | `AudioSession.ts:2288-2299` | Calls `recoverJsonDaemonBackend('play', true)` then `backend.resume()`, sets state `playing`, emits status, returns current status. If recovery fails, throws `json_daemon_unavailable:play` or `json_daemon_stale_backend:play`. | Preserve daemon resume fast path with recovery gate. | Covered indirectly by AudioCore.test.ts integration suite (json daemon recovery/stale backend scenarios) |
| `jsonrpc_transport.play_non_daemon_or_not_paused`: `play()` not on jsonrpc backend or not paused. | `AudioSession.ts:2288-2299` | Throws `json_daemon_unavailable:play`; no legacy fallback. | Preserve early rejection of non-paused or non-daemon play; `recoverJsonDaemonBackend()` must succeed before resume. | Covered indirectly by AudioCore.test.ts json daemon recovery/stale backend scenarios. |
| `jsonrpc_transport.pause_daemon`: `pause()` sees `currentOutputBackend === 'jsonrpc'` and backend exists. | `AudioSession.ts:2427-2439` | Calls `backend.pause()`, updates position, sets state `paused`, host status `ready`, resets watchdog progress, emits status, returns current status. | Preserve daemon pause path and watchdog reset. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `jsonrpc_transport.pause_non_daemon`: `pause()` not on jsonrpc backend or backend absent. | `AudioSession.ts:2509-2524` | Throws `json_daemon_unavailable:pause`; no legacy fallback. | Preserve early rejection of non-playing pause; `recoverJsonDaemonBackend()` must succeed before pause. | Covered indirectly by AudioCore.test.ts json daemon recovery/stale backend scenarios. |
| `jsonrpc_transport.seek_daemon_success`: `seek()` sees `currentOutputBackend === 'jsonrpc'` and `backend.seek()` succeeds. | `AudioSession.ts:2581-2597` | Position is clamped to track duration first; daemon `seek()` receives clamped position; local clock resets to clamped position using current probe sample rate or 48000; status emitted and returned. | Preserve successful daemon seek as terminal path. | `JsonRpcBridge.test.ts:173-199` covers daemon operation position gating; Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery scenarios) |
| `jsonrpc_transport.seek_daemon_failure`: daemon `backend.seek()` throws. | `AudioSession.ts:2664-2689` | Error is logged via `console.error`; method throws `json_daemon_unavailable:seek` with the daemon error as cause. Safe position is clamped to track duration bounds before the seek attempt. | Preserve safe position clamping and explicit `json_daemon_unavailable:seek` error; no legacy fallback. | Covered indirectly by AudioCore.test.ts integration suite (json daemon recovery/stale backend scenarios) |
| `jsonrpc_transport.local_play_daemon_success`: active usable daemon backend exists and `backend.openFile()` returns `status` `decoding`, `probed`, or `playing`. | `AudioSession.ts:1972-1999` | Session fields are populated from daemon metadata; state `playing`; host `ready`; output backend `jsonrpc`; daemon sample rate recorded; clock reset to daemon-normalized `startSeconds` or request start; EQ sync runs before returning status. Native daemon cache hits may already report `playing` and remain terminal JSON-RPC playback. | Preserve daemon success as terminal path and EQ sync after status emit, independent of low-load mode. | `AudioBackendContract.test.ts` covers default daemon playback with low-load disabled and cache-hit `playing` metadata; `JsonRpcBridge.test.ts` covers normalized daemon start positions. |
| `jsonrpc_transport.local_play_daemon_open_failed_or_unaccepted_status`: daemon `openFile()` throws, or returns status other than `decoding`/`probed`/`playing`. | `AudioSession.ts:2019-2071` | Errors are breadcrumbed/logged via `markPlaybackBreadcrumb`; method throws `json_daemon_unavailable:openFile` with the daemon error as cause; unaccepted daemon status also results in a thrown `json_daemon_unavailable:openFile` error. | Preserve daemon open failure as explicit `json_daemon_unavailable:openFile` error; no legacy fallback. | Covered indirectly by AudioCore.test.ts integration suite (json daemon recovery/stale backend scenarios) |
| `native_reuse.pending_graceful_stop`: `NativeOutputBridge.canReuseFor()` called while `pendingGracefulStop` is set. | `NativeOutputBridge.ts:1128-1131` | Returns false; AudioSession starts/replaces output instead of reusing resident host. | Preserve no reuse during graceful stop. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `native_reuse.asio`: start options normalize to ASIO. | `NativeOutputBridge.ts:1133-1136`, `AudioSession.ts:4855-4924` | `canReuseFor()` returns false; AudioSession labels restart reason `asio_session_rotation` when prior ready host exists. | Preserve ASIO session rotation/no resident reuse. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `native_reuse.eligible`: bridge ready, process/stdin exist and are writable, not ended/destroyed, and reuse key matches start options. | `NativeOutputBridge.ts:1138-1146`, `AudioSession.ts:4887-4910` | AudioSession reuses resident bridge only when not DirectSound, output mode allows resident reuse, `currentReadyResult` exists, and bridge `canReuseFor()` returns true; it recalculates resident plan/rate, reattaches events, returns `hostReused: true`, `hostRestartReason: null`. | Preserve all reuse gates and resident sample-rate replan. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `native_reuse.ineligible`: directsound backend, output mode disallows resident reuse, no `currentReadyResult`, missing/unwritable stdin/proc, not ready, or reuse key mismatch. | `NativeOutputBridge.ts:1138-1146`, `AudioSession.ts:4887-4934` | AudioSession does not reuse; waits for pending stop if needed, assigns restart reason (`reuse_key_changed`, `asio_session_rotation`, `resident_host_not_ready`, or `initial_start`), then stops/detaches old bridge before starting a new one. | Preserve restart reason selection and replacement behavior. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `prepared_playback.store`: local prepare completes. | `AudioSession.ts:266-267`, `AudioSession.ts:1680-1692`, `AudioSession.ts:4025-4037` | Prepared item is stored with `expiresAt = now + 120000ms`; cache prunes expired entries and evicts oldest entries above 50. | Preserve 2-minute TTL and 50-item LRU-ish cap. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `prepared_playback.take_hit`: play request matches cache key and item is unexpired. | `AudioSession.ts:3980-4014`, `AudioSession.ts:4040-4075` | Cache entry is moved to most-recent position; returns merged probe hints and age in ms; caller logs/use records prepared probe metadata. | Preserve exact cache-key dimensions and one-entry recency update. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `prepared_playback.take_miss_or_expired`: no matching key or item expired. | `AudioSession.ts:4017-4023`, `AudioSession.ts:4044-4058` | Expired entries are deleted; miss logs `local_prepare_cache_miss` only in verbose mode; returns null. | Preserve null result and no fallback warning on miss. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `asio_cooldown.remember`: ASIO unavailable error is seen during native fallback or ASIO output start while fallback is enabled. | `AudioSession.ts:328`, `AudioSession.ts:4615-4619`, `AudioSession.ts:4766-4771`, `AudioSession.ts:5035-5048` | Device key is stored with error message and `expiresAt = Date.now() + 30000`. | Preserve 30-second cooldown per normalized ASIO device key. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `asio_cooldown.hit`: starting ASIO with fallback enabled and unexpired unavailable record exists. | `AudioSession.ts:4796-4813` | Adds warnings `asio_output_device_temporarily_unavailable` and `asio_output_fell_back_to_safe_shared`; logs; immediately starts safe shared fallback without retrying ASIO. | Preserve skip-to-safe-shared behavior while cooldown is active. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `asio_cooldown.expired_or_absent`: unavailable ASIO record absent or expired. | `AudioSession.ts:4622-4633`, `AudioSession.ts:4796-4816` | Expired record is deleted; normal ASIO candidate loop proceeds. | Preserve automatic cooldown expiry cleanup. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `operation_gate.no_active_operation_id`: daemon has not accepted any numeric finite operation id. | `DaemonAudioBackend.ts:15`, `DaemonAudioBackend.ts:131-139` | `isCurrentOperation()` returns true for all notifications; position/ended events are accepted. | Preserve permissive pre-operation behavior. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `operation_gate.active_operation_match`: notification `operationId` equals active operation id. | `DaemonAudioBackend.ts:18-27`, `DaemonAudioBackend.ts:61-68`, `DaemonAudioBackend.ts:131-139` | Position updates to `positionBaseSeconds + framesPlayed / sampleRate`; ended callbacks fire. | Preserve active-operation-only event acceptance. | `JsonRpcBridge.test.ts:186-200` covers stale ignored and matching accepted. |
| `operation_gate.active_operation_mismatch`: notification `operationId` differs from active operation id. | `DaemonAudioBackend.ts:18-27`, `DaemonAudioBackend.ts:137-139` | Position and ended notifications are ignored. | Preserve stale daemon notification suppression. | `JsonRpcBridge.test.ts:186-200`, `JsonRpcBridge.test.ts:203-244`. |
| `operation_gate.operation_serialization`: open/seek/stop operations overlap. | `DaemonAudioBackend.ts:51-53`, `DaemonAudioBackend.ts:80-97`, `DaemonAudioBackend.ts:125-129` | `operationLane` serializes operations; later operations wait for prior operation settlement; active operation id is updated in lane order. | Preserve serialized operation lane. | `JsonRpcBridge.test.ts:203-244` covers stop before rapid second open. |
| `run_token.new_run`: playback/output-reset operations increment `runToken`. | `AudioSession.ts:1400`, `AudioSession.ts:1831`, `AudioSession.ts:1879`, `AudioSession.ts:2007-2008`, `AudioSession.ts:2164`, `AudioSession.ts:2455`, `AudioSession.ts:2513`, `AudioSession.ts:2534` | New token invalidates older async work, event handlers, prewarms, and recovery attempts. | Preserve monotonic run invalidation. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `run_token.assert_stale`: async path calls `assertCurrentRun(token)` after awaits and token changed. | `AudioSession.ts:2072`, `AudioSession.ts:4754`, `AudioSession.ts:4817`, `AudioSession.ts:4914`, `AudioSession.ts:4932`, `AudioSession.ts:4944`, `AudioSession.ts:5067`, `AudioSession.ts:5191`, `AudioSession.ts:5262`, `AudioSession.ts:7031-7034` | Throws `audio_session_run_cancelled`; callers commonly stop superseded bridge or treat as cancellation. | Preserve thrown sentinel string for stale async runs. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `run_token.event_stale`: bridge/decoder/prewarm callback token no longer matches. | `AudioSession.ts:5522-5524`, `AudioSession.ts:5540-5547`, `AudioSession.ts:5594-5597`, `AudioSession.ts:5613-5615`, `AudioSession.ts:5665-5667`, `AudioSession.ts:6061-6074`, `AudioSession.ts:6154-6169`, `AudioSession.ts:6253-6259` | Callback returns early; where a fresh decoder run was created, it is stopped; stale position/error/end work is ignored. | Preserve early-return/cleanup behavior for stale callbacks. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `watchdog.timer_disabled`: constructor dependency `disableWatchdogTimer` is true. | `AudioSession.ts:1533-1539` | Watchdog interval and event-loop lag monitor are not started. | Preserve opt-out timer behavior for tests/controlled sessions. | `AudioCore.test.ts` commonly constructs sessions with timer disabled. |
| `watchdog.not_monitorable`: not playing, already recovering, shared stability recovery in progress, bridge/current file/settings missing, or bridge position non-finite. | `AudioSession.ts:3053-3072` | Watchdog progress is reset and no recovery occurs. | Preserve reset-without-recovery behavior. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `watchdog.progress`: bridge position advances by more than 0.05 seconds. | `AudioSession.ts:272`, `AudioSession.ts:3075-3081` | Last position is updated and stalled-check count resets to 0. | Preserve epsilon progress threshold. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `watchdog.stall_below_threshold`: position does not advance but stalled check count is below configured threshold. | `AudioSession.ts:268-271`, `AudioSession.ts:1508-1514`, `AudioSession.ts:3084-3087` | Stalled count increments; no recovery yet. Defaults: interval 2000ms, checks 3, max recoveries 3, window 300000ms; dependency values clamp to interval >=250, checks >=1, max >=0, window >=1000. | Preserve threshold and constructor clamps. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `watchdog.stall_threshold_reached`: stalled checks reach threshold. | `AudioSession.ts:3084-3090`, `AudioSession.ts:7942-7964` | `recoverFromWatchdogStall()` runs if not already recovering; live PCM skips restart; normal local playback delegates to `recoverOutputStability('audio_watchdog_recovered_native_output', position, token)`. | Preserve one recovery attempt per threshold crossing. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `watchdog.recovery_window_fresh_or_reset`: no current recovery record for track/file, or record window is older than `watchdogRecoveryWindowMs`. | `AudioSession.ts:7350-7361`, `AudioSession.ts:7376-7387` | Recent count reports 0; reservation starts a new window with count 0 then increments. | Preserve per-track/file recovery window reset. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `watchdog.recovery_limited`: recovery count in current window is >= `watchdogMaxRecoveriesPerTrack`. | `AudioSession.ts:7364-7373`, `AudioSession.ts:7389-7396`, `AudioSession.ts:7841-7848` | Diagnostics show `limited` while playing; recovery reservation returns null; warning `shared_stability_recovery_limited` or `native_output_stability_recovery_limited` is added; status emitted; playback is not restarted by watchdog. | Preserve hard recovery cap. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `watchdog.recovery_allowed_shared`: recovery slot reserved for shared output. | `AudioSession.ts:7497-7524`, `AudioSession.ts:7841-7917` | Shared tier becomes `recovery` for first recovery, `emergency` for second or later; output restarts at safe position with adjusted latency/buffer; warnings include original reason, `shared_stability_recovered:N`, and `native_output_buffer_recovered:<target>`. | Preserve tier escalation and restart warnings. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `watchdog.recovery_allowed_non_shared`: recovery slot reserved for non-shared output. | `AudioSession.ts:7497-7524`, `AudioSession.ts:7841-7917` | Output restarts at safe position with low-latency buffer 1024 on first recovery, 2048 on second, and stable profile at third or later; warnings include `native_output_stability_recovered:N`. | Preserve non-shared buffer escalation. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `shared_stability.remember`: shared watchdog stability recovery chooses a tier. | `AudioSession.ts:326`, `AudioSession.ts:6660-6714`, `AudioSession.ts:7865-7868` | A shared-route key is stored with tier and 30-minute expiry; `lastSharedStabilityRecoveryKey` is updated. | Preserve 30-minute shared stability memory. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `shared_stability.apply_fresh_playback`: fresh shared playback begins and no recovery is currently active. | `AudioSession.ts:2055`, `AudioSession.ts:6716-6735` | `sharedStabilityTier` is restored from unexpired memory; otherwise recent same-device recovery within 30 minutes is reused; otherwise resets to `standard`. | Preserve remembered tier priority over recent same-device fallback over standard. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `shared_stability.clear_on_manual_route_or_latency_change`: setOutput includes latency, buffer, device index, or device name. | `AudioSession.ts:1751-1758`, `AudioSession.ts:6643-6649` | Shared stability memory is cleared; last recovery key cleared; tier resets to `standard` unless currently recovering. | Preserve manual setting changes as memory invalidation. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `eq_sync.rpc_bridge_not_open`: EQ sync throws `rpc_bridge_not_open`, `eq_control_closed`, or `eq_control_disconnected`. | `AudioSession.ts:960-964`, `AudioSession.ts:7296-7319`, `JsonRpcBridge.ts:211` | Error is classified recoverable; warning `eq_control_sync_skipped` is added; log emitted; recoverable audio error reported with `phase='eq-control-sync'` and `recovered: true`; playback start is not failed. | Preserve recoverable EQ sync classification and warning. | `JsonRpcBridge.test.ts:96-99`, `JsonRpcBridge.test.ts:321-327` cover `rpc_bridge_not_open`; Covered indirectly by AudioCore.test.ts integration suite (EQ sync/recovery scenarios) |
| `eq_sync.non_disconnect_error`: EQ sync throws any other error. | `AudioSession.ts:7309-7312` | Error is rethrown; playback path can fail. | Preserve strict rethrow for non-disconnect EQ errors. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `startup_fallback.exclusive_sample_rate_mismatch_fallback_allowed`: PCM stream/output ready detects exclusive actual/requested mismatch and shared fallback is enabled. | `AudioSession.ts:2074-2105`, `AudioSession.ts:4434-4445`, `AudioSession.ts:5248-5320` | Starts shared fallback, updates bridge/plan/ready/reuse fields, then applies ready result and continues playback. | Preserve exclusive mismatch shared fallback. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `startup_fallback.exclusive_sample_rate_mismatch_fallback_blocked`: same mismatch but fallback disabled. | `AudioSession.ts:2074-2090`, `AudioSession.ts:5055-5064` | Adds `exclusive_output_fallback_blocked`, logs, and throws original mismatch/start error. | Preserve blocked fallback warning and thrown failure. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `startup_fallback.shared_selected_device_failed_default_blocked`: shared selected device fails and default-device fallback disabled. | `AudioSession.ts:5132-5140` | Adds `shared_output_default_device_fallback_blocked`, logs, throws last error. | Preserve no implicit default fallback. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `startup_fallback.asio_failed_safe_shared_allowed`: ASIO output fails and ASIO unavailable + default fallback are enabled. | `AudioSession.ts:765-766`, `AudioSession.ts:5150-5171` | Adds `asio_output_fell_back_to_safe_shared`, logs, starts safe shared fallback; safe fallback sets shared stability tier `emergency` and warning `shared_output_recovered_safe_mode`. | Preserve safe shared ASIO fallback. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
| `startup_fallback.asio_failed_fallback_blocked`: ASIO output fails but safe shared fallback is not allowed. | `AudioSession.ts:5150-5158` | Adds `asio_output_fallback_blocked`, logs, throws last error. | Preserve blocked ASIO fallback. | Covered indirectly by AudioCore.test.ts integration suite (daemon playback/recovery/fallback scenarios) |
